@using YourFavECommerce.Utilites
@using YourFavECommerce.ViewModels
@model UserWithRelatedVM

@* <h1>@Context.Request.Cookies["success-notifications"]</h1> *@

@* @section Scripts {
    @if (@TempData["success-notifications"] is not null)
    {
        <script>
            toastr.success('@TempData["success-notifications"]')
        </script>
    }
} *@

<div class="container">
    <div class="row mb-3 align-items-center my-3">
        <div class="col-md-6">
            <h2 class="display-6">All Users</h2>
        </div>
        <div class="col-md-6 text-md-end text-start mt-2 mt-md-0">
            <a href="/admin/user/create" class="btn btn-success">Create</a>
        </div>
    </div>

    <!-- Filter Section -->
    <section class="py-3 filter-bar">
        <div class="row" style="background-color: rgba(var(--bs-light-rgb), var(--bs-bg-opacity)) !important;">
            <div class="col-12">
                <form>
                    <div class="row">
                        <!-- User Name -->
                        <div class="col-md-3">
                            <input type="text" class="form-control" placeholder="User Name" name="name">
                        </div>

                        <!-- Status -->
                        @* <div class="col-md-2">
                            <select name="status" class="form-control">
                                <option value="true">Active</option>
                                <option value="false">IN Active</option>
                            </select>
                        </div> *@

                        <!-- Submit Button -->
                        <div class="col-md-2">
                            <button type="submit" class="btn btn-dark w-100">Filter</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </section>

    <div class="row">
        <div class="col-12">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th scope="col">Name</th>
                        <th scope="col">Email</th>
                        <th scope="col">Email Confirmed</th>
                        <th scope="col">Status</th>
                        <th scope="col">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.ApplicationUsers)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>@item.Email</td>

                            @if (item.EmailConfirmed)
                            {
                                <td><span class="badge text-bg-success">Confirmed</span></td>
                            }
                            else
                            {
                                <td><span class="badge text-bg-danger">NOT Confirmed</span></td>
                            }

                            @if (item.LockoutEnabled)
                            {
                                <td><span class="badge text-bg-success">Active</span></td>
                            }
                            else
                            {
                                <td><span class="badge text-bg-danger">Blocked!!</span></td>
                            }
                            <td>
                                <a href="/admin/user/edit?id=@item.Id" type="button" class="btn btn-warning">Edit</a>

                                @if (item.LockoutEnabled)
                                {
                                    <a href="/admin/user/lockUnLock?id=@item.Id" class="btn btn-danger btn-block">Block</a>
                                }
                                else
                                {
                                    <a href="/admin/user/lockUnLock?id=@item.Id" class="btn btn-warning btn-block">Un Block</a>
                                }

                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <!-- Pagination -->
    <div class="row justify-content-center">
        <div class="col-md-12 justify-content-center">
            <nav aria-label="Page navigation example">
                <ul class="pagination" style="
                    display: flex;
                    justify-content: center;>
                    <li class="page-item">
                        <a class="page-link" href="?page=@(Model.CurrentPage - 1)" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                        </a>
                    </li>
                    @for (var i = 1; i <= @Model.TotalPages; i++)
                    {
                        if (i == Model.CurrentPage)
                        {
                            <li class="page-item active"><a class="page-link" href="?page=@i">@i</a></li>

                        }
                        else
                        {

                            <li class="page-item"><a class="page-link" href="?page=@i">@i</a></li>
                        }

                    }
                    <li class="page-item">
                        <a class="page-link" href="?page=@(Model.CurrentPage + 1)" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </div>
    </div>
</div>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const deleteButtons = document.querySelectorAll(".btn-block");

        deleteButtons.forEach(button => {
            button.addEventListener("click", function (e) {
                e.preventDefault(); // stop normal navigation

                const url = this.getAttribute("href");

                Swal.fire({
                    title: "Are you sure?",
                    text: "This action cannot be undone!",
                    icon: "warning",
                    showCancelButton: true,
                    confirmButtonColor: "#d33",
                    cancelButtonColor: "#3085d6",
                    confirmButtonText: "Yes, block it!"
                }).then((result) => {
                    if (result.isConfirmed) {
                        Swal.fire({
                          title: "Blocked!",
                          text: "The user has been blocked.",
                          icon: "success"
                        });

                        // Redirect to delete URL
                        window.location.href = url;
                    }
                });
            });
        });
    });
</script>


